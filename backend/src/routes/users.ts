import { Router } from 'express';
import { auth } from '../middleware/auth';

const router = Router();

// User profile routes would go here
// For now, user profile functionality is handled in auth routes

export default router;
